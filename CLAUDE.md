# 网银项目 — Online Banking System
## 关于我
 
计算机专业应届生，主攻Java后端开发，使用Claude Code完成银行展示网银项目开发、代码编写、数据库设计、接口调试、文档撰写与项目规范化落地。
 
## 思维原则
 
所有决策从问题本质出发，不因「惯例如此」照搬。回到问题本身：要解决什么？最直接的路径是什么？从零设计会怎么做？不要谄媚。不要夸想法、不要说客套话术、不要开头用礼貌铺垫语。给出真实判断，方案存在缺陷直接指出，有更优实现方式直接给出，无需等待提问。
 
## 约束先行
 
开发项目优先建立项目规范与文档，本项目严格遵循目录命名、类名、方法名、数据库字段统一规范。无统一规范不编写业务代码，已有设计文档严格按照文档实现功能。如需调整表结构、业务逻辑、接口字段，优先修改设计文档，再修改代码实现，禁止反向修改。项目素材目录创建受文档约束，目录创建节点严格遵照文档时序执行。
 
## 沟通方式
 
日常交流使用中文，代码、变量、包名、命令、数据库字段统一使用英文。输出内容结论先行，附带实现理由，不冗余铺垫背景信息。需求模糊时直接给出最优通用企业级实现方案，无需反复确认。非红线操作不主动询问确认。
 
## 自主边界（红线，必须提前确认）
 
以下操作全自动模式下也必须暂停等待确认：
 
- 删除项目业务代码、目录结构、Git提交记录
- 修改.env配置、数据库账号密码、项目密钥、接口令牌
- 整体变更数据库表结构、批量数据迁移、字段大范围修改
- Git推送、变基、强制回退等版本高危操作
- 本地全局安装新依赖、修改系统运行环境
- 项目打包部署、线上发布、公开上传代码至仓库

## 协作约定
1. 开发模式：自主迭代开发，常规实现细节自主处理，不琐碎提问；
2. 仅以下关键场景向开发者确认：
   - 数据库表字段/关联关系大幅修改
   - 整体业务逻辑和需求文档不一致
   - 更换技术组件、框架方案
   - 新增原有设计没有的功能模块
3. 单个模块开发完毕，输出：模块完成说明+修改文件列表+接口清单
 
## 通用工程纪律
 
每完成一个功能模块主动进行接口测试、功能自测，完成验证再进行下一模块开发。禁止注释报错代码、绕过异常逻辑完成功能，必须定位根源解决问题。所有账号密码、连接密钥统一存放环境变量，严禁写入代码、配置类、提交记录与日志中。涉及业务大模块改动，先输出开发方案，确认无误后再编码实现。项目图片资源不可自行新增、移动、删除本地图片文件，仅负责代码路径编写。

## 敏感配置强制规则
1. spring-datasource数据库url、username、password、驱动配置，Redis密码、API密钥、accessKey等隐私信息，**绝不允许AI自行编造默认值、硬编码写入yml/properties**；
2. 碰到需要填充敏感密码、密钥的代码位置，暂停编码，主动弹窗向我索要配置内容；
3. 非敏感配置（端口、项目名、分页默认条数）可自行按规范填写。
 
## 项目专属开发约束
 
1. 整体采用SpringBoot3+Java21 + MyBatis-Plus企业主流架构，代码风格贴合职场后端开发规范，前端使用Vue3+Vite技术栈。
2. 后端统一封装全局返回结果、全局异常处理器、分页工具类，优先完成基础通用组件后再开发业务代码。
3. 所有业务数据表统一配置软删除字段，全业务逻辑使用软删除，配套回收站查询、管理员物理彻底删除功能。
4. 实现RBAC五级权限控制，区分系统运维管理员、安全审计员、业务配置管理员、普通运营管理员、前台普通用户五类权限，遵循银行标准岗位分离原则，无全能型超级管理员。
5. 所有业务接口添加入参注解校验，全部接口补充标准JavaDoc注释，便于前后端联调。
6. 日志记录关键操作行为，包含用户注册登录、产品增删改、申请提交、数据恢复/彻底删除等操作。
7. 项目开发时序约束：前后端项目目录初始化完成后，创建文档约定的全部分类图片文件夹，立刻暂停开发并提示用户放入对应图片，等待用户确认素材全部就位后，再继续剩余页面、接口编码；开发阶段代码内使用规范路径占位，不自行填充图片资源。
 
## 补充约束
 
1. 图片文件夹层级变更、新增素材分类，必须先修订项目设计文档，文档更新完成后方可修改代码路径；
2. 可自主拓展优化功能，新增功能需要在设计文档对应位置补充说明。

## 项目概述

前后端分离的网银系统，提供产品展示、在线申请、转账交易、后台管理等功能。遵循银行标准五层角色架构，实现权限相互隔离、各司其职。

## 技术栈

| 层级 | 技术 |
|------|------|
| 后端框架 | Java 21 + SpringBoot 3.x + MyBatis-Plus |
| 构建工具 | Maven（Maven Wrapper） |
| 前端框架 | Vue 3 + Vite + VueRouter + Pinia + ElementPlus + Axios |
| 数据库 | MySQL 9.6（库名 `bank_demo`，字符集 `utf8mb4`） |
| 认证 | JWT（密钥存环境变量） |
| 权限 | RBAC 五级：系统运维 / 安全审计 / 业务配置 / 运营管理 / 普通用户 |

## 项目结构

```
bank-system/
├── backend/                    # 后端模块
│   ├── src/main/java/com/bank/
│   │   ├── common/             # 通用封装（Result、异常、分页）
│   │   ├── config/             # 跨域、JWT、MyBatis-Plus 配置
│   │   ├── module/
│   │   │   ├── user/           # 用户模块（注册、登录）
│   │   │   ├── product/        # 产品模块
│   │   │   ├── banner/         # 轮播模块
│   │   │   ├── apply/          # 申请模块
│   │   │   ├── card/           # 银行卡模块
│   │   │   ├── news/           # 新闻公告模块
│   │   │   ├── dashboard/      # 数据看板
│   │   │   └── transaction/    # 交易模块
│   │   ├── rbac/               # RBAC 权限体系
│   │   └── log/                # 操作日志
│   └── src/main/resources/
│       └── static/bankImg/     # 图片资源虚拟目录（banner/card/finance/loan/pub）
├── frontend/                   # 前端模块
│   └── src/
│       ├── api/                # Axios 接口封装（request.js 实例 + 模块化 API）
│       ├── assets/
│       │   ├── styles/         # 全局样式（CSS 变量体系）
│       │   └── (images)        # 静态图片资源
│       ├── components/         # 公共组件（AppHeader/AppFooter/ProductList/info/）
│       ├── composables/        # Vue3 组合式函数（use-image）
│       ├── router/             # 路由配置 + 全局导航守卫
│       ├── stores/             # Pinia 状态管理
│       ├── utils/              # 工具函数 + 全局常量
│       └── views/              # 页面（统一 index.vue 命名）
└── CLAUDE.md
```

## 数据库规范

- 所有业务表统一字段：`deleted`, `create_by`, `update_by`, `create_time`, `update_time`
- 软删除：`deleted` = 0 正常，1 已删除；查询默认过滤已删数据
- 表清单：`sys_user`, `bank_product`, `banner`, `apply_info`, `bank_news`, `bank_card`, `transaction_record`, `sys_role`, `sys_user_role`, `sys_menu`

## 编码规范

- **统一返回格式：** `Result<T>` {code, msg, data}
- **全局异常处理：** 拦截系统异常、参数异常、业务异常
- **参数校验：** 注解式校验（JSR 303），非法参数直接拦截
- **接口注释：** 所有 Controller 方法加 JavaDoc
- **软删除：** 提供回收站查询、恢复、物理删除能力
- **操作日志：** 注册登录、产品增删改、申请提交、数据恢复/删除均记录
- **安全机制：** 密码 BCrypt 加密、交易密码独立 6 位数字、登录失败 5 次锁定 30 分钟、敏感信息脱敏展示、前后端双重权限拦截
- **敏感脱敏：** 银行卡号、手机号、身份证号全场景掩码展示（前 N 位 + **** + 后 N 位）

## 核心工作流

1. **文档先行：** 任何表结构、接口、业务逻辑调整 → 先修订设计文档 → 再改代码（禁止反向操作）
2. **单模块自测：** 每完成一个接口，用测试工具自测通过后再开发下一个
3. **红线暂停：** 以下操作触发即暂停，确认后再继续：
   - 删除后端业务代码 / 项目目录 / Git 提交记录
   - 修改数据库连接、密钥、JWT 令牌、环境变量
   - 全表字段调整、大批量数据迁移
   - Git 推送、强制回退、变基
   - 项目打包发布、代码上传云端仓库

## 开发命令

```bash
# 后端启动（Maven Wrapper）
cd backend
./mvnw spring-boot:run

# 后端打包
./mvnw clean package

# 前端启动
cd frontend
npm install
npm run dev
```

## 环境配置

- 后端端口：8080（默认）
- 前端端口：5173（默认）
- 敏感配置（MySQL 连接、账号密码、JWT 密钥、跨域白名单）统一存环境变量，不硬编码

## 路由清单

| 路径 | 页面 | 权限 |
|------|------|------|
| `/` | 首页 | 公开 |
| `/personal` | 个人业务 | 公开 |
| `/credit` | 信用卡 | 公开 |
| `/company` | 公司金融 | 公开 |
| `/puhui` | 普惠金融 | 公开 |
| `/info` | 信息公开 | 公开 |
| `/news` | 新闻公告列表 | 公开 |
| `/news/detail` | 新闻详情 | 公开 |
| `/login` | 登录 | 公开 |
| `/register` | 注册 | 公开 |
| `/apply/card` | 信用卡申请表 | 需登录 |
| `/apply/loan` | 贷款申请表 | 需登录 |
| `/user/center` | 个人中心 | 需登录 |
| `/transfer` | 转账汇款 | 需登录 |
| `/calculator` | 贷款计算器 | 公开 |
| `/forgot-password` | 忘记密码 | 公开 |
| `/product/detail` | 产品详情 | 公开 |
| `/admin` | 后台管理 | 管理员/运营（按角色分权） |

## 项目开发顺序

1. 文档定稿 → 命名/数据库规范
2. MySQL 建表 + 预置测试数据
3. 后端通用封装（Result、异常、分页、RBAC 基础）
4. 按模块开发：用户 → 产品 → 轮播 → 申请 → 银行卡 → 新闻 → 看板 → 交易
5. 前端项目搭建（路由、Axios、Pinia）
6. 页面开发：首页 → 业务页 → 登录注册 → 申请表 → 个人中心 → 转账 → 后台管理
7. 联调复测 + 优化补充

## 默认端口约定

| 服务 | 端口 |
|------|------|
| 后端 | 8080 |
| 前端 | 5173 |
| MySQL | 3306 |

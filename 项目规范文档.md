# 网银项目 — 命名规范与数据库规范

> 本文档为项目编码基准，任何表结构、字段、API 路径、目录结构的变更，必须先修订本文档，再改代码。

---

## 一、命名规范

### 1.1 后端 Java 命名

| 维度 | 规范 | 示例 |
|------|------|------|
| 根包 | `com.bank` | `com.bank` |
| 模块分包 | `com.bank.module.{模块名}` | `com.bank.module.user` |
| 通用分包 | `com.bank.common` | 异常、返回、分页 |
| 配置分包 | `com.bank.config` | 跨域、JWT、MP 配置 |
| Controller | `XxxController` | `UserController` |
| Service 接口 | `XxxService` | `UserService` |
| Service 实现 | `XxxServiceImpl` | `UserServiceImpl` |
| Mapper | `XxxMapper` | `UserMapper` |
| 实体 | `Xxx`（无后缀） | `User`, `BankProduct` |
| VO / DTO | `XxxVo`, `XxxDto` | `UserVo`, `ApplyDto` |
| 自定义异常 | `XxxException` | `BusinessException` |
| 枚举 | `XxxEnum` | `ProductTypeEnum` |

### 1.2 数据库命名

| 维度 | 规范 | 示例 |
|------|------|------|
| 数据库名 | 小写字母 + 下划线 | `bank_demo` |
| 表名 | 小写字母 + 下划线 | `sys_user`, `bank_product` |
| 字段名 | 小写字母 + 下划线 | `product_name`, `create_time` |
| 主键 | `id` | `id BIGINT AUTO_INCREMENT` |
| 逻辑删除 | `deleted` | `deleted TINYINT(1)` |
| 时间字段 | `_time` 后缀 | `create_time`, `update_time` |
| 人员字段 | `_by` 后缀 | `create_by`, `update_by` |
| 关联表 | `{A}_{B}` 中间用 `_` | `sys_user_role` |

### 1.3 API 路径规范

```
后端统一前缀（预留，当前可不加）：
  /api/xxx

实际接口路径：
  GET    /user/register          # 注册
  POST   /user/login             # 登录
  POST   /user/logout            # 退出
  GET    /product/list           # 产品分页列表
  GET    /product/detail/{id}    # 产品详情
  POST   /product/add            # 新增产品（管理员）
  PUT    /product/update         # 修改产品（管理员）
  DELETE /product/delete/{id}    # 软删除产品（管理员）
  GET    /product/recycle        # 回收站列表（管理员）
  POST   /product/recover/{id}   # 恢复产品（管理员）
  DELETE /product/wipe/{id}      # 物理删除（管理员）
  GET    /banner/list            # 轮播列表
  POST   /banner/add             # 新增轮播（管理员）
  PUT    /banner/update          # 修改轮播（管理员）
  DELETE /banner/delete/{id}     # 软删除轮播（管理员）
  POST   /apply/submit           # 提交申请（需登录）
  GET    /apply/list             # 申请分页（管理员看全部，普通用户看自己的）
  PUT    /apply/{id}             # 修改申请（管理员）
  DELETE /apply/{id}             # 软删除申请（管理员）
  POST   /apply/approve/{id}     # 审批申请（管理员）
  GET    /apply/recycle          # 申请回收站（管理员）
  POST   /apply/recover/{id}     # 恢复申请（管理员）
  DELETE /apply/wipe/{id}        # 物理删除申请（管理员）
  GET    /news/list              # 新闻分页列表（公开）
  GET    /news/detail/{id}       # 新闻详情（公开）
  POST   /news/add               # 新增新闻（管理员）
  PUT    /news/update            # 修改新闻（管理员）
  DELETE /news/delete/{id}       # 软删除新闻（管理员）
  GET    /news/recycle           # 新闻回收站（管理员）
  POST   /news/recover/{id}      # 恢复新闻（管理员）
  DELETE /news/wipe/{id}         # 物理删除新闻（管理员）
  GET    /card/list              # 我的银行卡列表（需登录）
  GET    /card/detail/{id}       # 银行卡详情（需登录）
  POST   /card/freeze/{id}       # 冻结银行卡（需登录）
  POST   /card/unfreeze/{id}     # 解冻银行卡（需登录）
  PUT    /user/profile           # 修改个人信息（需登录）
  PUT    /user/password          # 修改密码（需登录）
  PUT    /user/transaction-password  # 设置交易密码（需登录）
  POST   /user/forgot-password   # 忘记密码（公开）
  POST   /transaction/transfer   # 行内转账（需登录）
  GET    /transaction/list       # 交易流水（需登录）
  GET    /dashboard/stats        # 后台统计数据（管理员/运营）
```

### 1.4 前端命名

| 维度 | 规范 | 示例 |
|------|------|------|
| 组件名 | PascalCase | `ProductCard.vue` |
| 路由名 | camelCase | `personal`, `applyCard` |
| 目录名 | camelCase | `views`, `stores`, `api` |
| 图片资源 | 小写 + 连字符 | `logo.svg`, `default-bg.png` |

---

## 二、数据库规范

### 2.1 通用审计字段（所有业务表必备）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `deleted` | `TINYINT(1)` | 逻辑删除：0=正常，1=已删除，默认 0 |
| `create_by` | `VARCHAR(64)` | 创建人，默认空字符串 |
| `update_by` | `VARCHAR(64)` | 更新人，默认空字符串 |
| `create_time` | `DATETIME` | 创建时间，默认 CURRENT_TIMESTAMP |
| `update_time` | `DATETIME` | 更新时间，默认 CURRENT_TIMESTAMP，ON UPDATE |

### 2.2 表结构详述

#### ① sys_user — 用户信息表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| phone | VARCHAR(20) | UNIQUE NOT NULL | 手机号（登录账号） |
| password | VARCHAR(255) | NOT NULL | 登录密码 BCrypt |
| nick_name | VARCHAR(64) | NOT NULL | 昵称 |
| real_name | VARCHAR(64) | DEFAULT '' | 真实姓名 |
| id_card | VARCHAR(18) | DEFAULT '' | 身份证号 |
| transaction_password | VARCHAR(255) | DEFAULT '' | 交易密码 BCrypt（6位数字） |
| login_attempts | INT | DEFAULT 0 | 连续登录失败次数 |
| locked_at | DATETIME | DEFAULT NULL | 账号锁定截止时间 |
| role_id | BIGINT | NOT NULL DEFAULT 5 | 角色 ID（5=普通用户） |
| deleted | TINYINT(1) | DEFAULT 0 | 逻辑删除 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建人 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新人 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：** `uk_phone`(phone), `idx_role_id`(role_id)

#### ② bank_product — 产品信息表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| product_name | VARCHAR(128) | NOT NULL | 产品名 |
| product_type | TINYINT(2) | NOT NULL | 产品分类：1=个人业务，2=信用卡，3=公司金融，4=普惠金融 |
| rate | DECIMAL(5,2) | DEFAULT 0.00 | 年化利率 |
| min_amount | DECIMAL(12,2) | DEFAULT 0.00 | 起购金额 |
| term | VARCHAR(64) | DEFAULT '' | 期限说明 |
| risk_level | TINYINT(2) | DEFAULT 1 | 风险等级：1=低 2=中 3=高 |
| product_status | TINYINT(1) | DEFAULT 0 | 产品状态：0=正常 1=停售 |
| fee_desc | VARCHAR(255) | DEFAULT '' | 手续费说明 |
| intro | TEXT | | 产品介绍 |
| img_path | VARCHAR(255) | DEFAULT '' | 虚拟图片路径，如 `card/card01.png` |
| sort | INT | DEFAULT 0 | 排序权重（越小越前） |
| deleted | TINYINT(1) | DEFAULT 0 | 逻辑删除 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建人 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新人 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：** `idx_product_type`(product_type), `idx_sort`(sort)

#### ③ banner — 轮播图表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| img_path | VARCHAR(255) | NOT NULL | 虚拟图片路径，如 `banner/banner01.png` |
| jump_route | VARCHAR(255) | DEFAULT '' | 前端跳转路由 |
| sort | INT | DEFAULT 0 | 排序 |
| deleted | TINYINT(1) | DEFAULT 0 | 逻辑删除 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建人 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新人 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

#### ④ apply_info — 申请表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT | NOT NULL | 申请人 ID |
| product_id | BIGINT | NOT NULL | 产品 ID |
| real_name | VARCHAR(64) | NOT NULL | 姓名 |
| phone | VARCHAR(20) | NOT NULL | 联系号码 |
| apply_type | TINYINT(2) | NOT NULL | 申请类型：1=办卡，2=贷款 |
| status | TINYINT(2) | NOT NULL DEFAULT 0 | 审核状态：0=待审核，1=已通过，2=已拒绝 |
| remark | VARCHAR(500) | DEFAULT '' | 审核备注/拒绝原因 |
| approver_id | BIGINT | DEFAULT NULL | 审批人 ID |
| approve_time | DATETIME | DEFAULT NULL | 审批时间 |
| deleted | TINYINT(1) | DEFAULT 0 | 逻辑删除 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建人 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新人 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：** `idx_user_id`(user_id), `idx_product_id`(product_id), `idx_status`(status)

#### ⑤ bank_news — 新闻公告表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| title | VARCHAR(255) | NOT NULL | 标题 |
| summary | VARCHAR(500) | DEFAULT '' | 摘要 |
| content | TEXT | | 正文（支持 HTML） |
| status | TINYINT(1) | DEFAULT 1 | 发布状态：0=草稿，1=已发布 |
| sort | INT | DEFAULT 0 | 排序权重（越小越前） |
| deleted | TINYINT(1) | DEFAULT 0 | 逻辑删除 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建人 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新人 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：** `idx_status`(status), `idx_sort`(sort)

#### ⑥ bank_card — 银行卡表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT | NOT NULL | 持卡人 ID |
| apply_id | BIGINT | DEFAULT NULL | 关联申请 ID |
| card_number | VARCHAR(19) | NOT NULL | 卡号（19 位） |
| card_type | TINYINT(2) | NOT NULL | 卡类型：1=借记卡 2=信用卡 |
| credit_limit | DECIMAL(12,2) | DEFAULT 0.00 | 信用额度 |
| balance | DECIMAL(12,2) | DEFAULT 0.00 | 余额 |
| cvv | VARCHAR(3) | NOT NULL | CVV 安全码 |
| expiry_date | VARCHAR(5) | NOT NULL | 有效期 MM/YY |
| status | TINYINT(2) | NOT NULL DEFAULT 0 | 状态：0=正常 1=已冻结 2=已注销 |
| deleted | TINYINT(1) | DEFAULT 0 | 逻辑删除 |
| create_by | VARCHAR(64) | DEFAULT '' | 创建人 |
| update_by | VARCHAR(64) | DEFAULT '' | 更新人 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引：** `idx_user_id`(user_id), `idx_card_number`(card_number), `idx_status`(status)

**卡号生成规则：** `6228` + 时间戳后 10 位 + 5 位随机数 = 19 位唯一卡号

#### ⑦ transaction_record — 交易流水表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT | NOT NULL | 用户 ID |
| from_card_id | BIGINT | DEFAULT NULL | 付款卡 ID |
| to_account | VARCHAR(32) | NOT NULL | 收款账号/卡号 |
| amount | DECIMAL(12,2) | NOT NULL | 交易金额 |
| fee | DECIMAL(12,2) | DEFAULT 0.00 | 手续费 |
| type | TINYINT(2) | NOT NULL | 交易类型：1=转账 2=缴费 3=理财 4=还款 |
| status | TINYINT(2) | DEFAULT 1 | 状态：0=失败 1=成功 |
| remark | VARCHAR(255) | DEFAULT '' | 备注 |
| trade_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 交易时间 |
| deleted/create_by/update_by/create_time/update_time | 标准审计字段 | — |

**索引：** `idx_user_id`(user_id), `idx_trade_time`(trade_time)

#### ⑧ sys_role — 角色表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| role_name | VARCHAR(64) | NOT NULL | 角色名 |
| role_code | VARCHAR(64) | UNIQUE NOT NULL | 角色编码：`SUPER_ADMIN` / `OPERATOR` / `USER` |
| description | VARCHAR(255) | | 角色描述 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**初始数据：**
- `SYS_ADMIN`(1) — 系统运维管理员（后台账号管理、DB 备份恢复）
- `AUDITOR`(2) — 安全审计员（全量日志查看，零增删改）
- `BIZ_ADMIN`(3) — 业务配置管理员（产品/轮播/新闻 CRUD、风控配置）
- `OPERATOR`(4) — 普通运营管理员（用户管理、申请审核、交易查看）
- `USER`(5) — 普通个人用户（前台浏览、资金交易、个人中心）

#### ⑨ sys_user_role — 用户角色关联表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| user_id | BIGINT | NOT NULL | 用户 ID |
| role_id | BIGINT | NOT NULL | 角色 ID |

**索引：** `uk_user_role`(user_id, role_id) UNIQUE

#### ⑩ sys_menu — 菜单权限表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK AUTO_INCREMENT | 主键 |
| menu_name | VARCHAR(64) | NOT NULL | 菜单名 |
| permission | VARCHAR(128) | NOT NULL | 权限标识，如 `product:add`, `product:delete` |
| parent_id | BIGINT | DEFAULT 0 | 父菜单 ID |
| path | VARCHAR(255) | DEFAULT '' | 前端路由路径 |
| create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

---

## 三、MyBatis-Plus 配置规范

- 全局逻辑删除：`deleted` 字段，0=正常，1=已删除
- 自动填充：`create_time`, `update_time`, `create_by`, `update_by` 使用 MP 元对象处理器
- 分页：使用 MyBatis-Plus `PaginationInnerInterceptor`
- 查询时自动过滤 `deleted=0`（MP 逻辑删除默认行为）

---

## 四、RBAC 权限策略

| 角色 | 编码 | roleId | 后台账号 | 日志审计 | 产品/轮播/新闻CRUD | 申请审核 | 用户管理 | 回收站 | 物理删除 | 前台交易 |
|------|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 系统运维管理员 | SYS_ADMIN | 1 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 安全审计员 | AUDITOR | 2 | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 业务配置管理员 | BIZ_ADMIN | 3 | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 普通运营管理员 | OPERATOR | 4 | ❌ | ❌ | ❌ | ✅ | ✅ | ✅ | ❌ | ❌ |
| 普通个人用户 | USER | 5 | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |

---

## 五、图片资源虚拟目录

```
resources/static/bankImg/
├── banner/        # 轮播图
├── card/          # 信用卡产品图
├── finance/       # 公司金融产品图
├── loan/          # 贷款产品图
└── pub/           # 公共/兜底图
```

- 数据库 `img_path` 只存相对路径，如 `card/card01.png`
- 前端拼接完整 URL：`http://localhost:8080/bankImg/{img_path}`
- 物理目录由使用者自建填充，本仓库不存放图片文件
- 优化：前端封装 `getImageUrl(path)` 常量函数，图片加载失败显示兜底缺省图

---

## 六、前端路由清单

| 路径 | 页面 | 权限 |
|------|------|------|
| `/` | 首页 | 公开 |
| `/personal` | 个人业务 | 公开 |
| `/credit` | 信用卡 | 公开 |
| `/company` | 公司金融 | 公开 |
| `/puhui` | 普惠金融 | 公开 |
| `/info` | 信息公开 | 公开 |
| `/news` | 新闻公告列表 | 公开 |
| `/news/detail` | 新闻详情 | 公开 |
| `/login` | 登录 | 公开 |
| `/register` | 注册 | 公开 |
| `/apply/card` | 信用卡申请表 | 需登录 |
| `/apply/loan` | 贷款申请表 | 需登录 |
| `/user/center` | 个人中心 | 需登录 |
| `/transfer` | 转账汇款 | 需登录 |
| `/calculator` | 贷款计算器 | 公开 |
| `/forgot-password` | 忘记密码 | 公开 |
| `/product/detail` | 产品详情 | 公开 |
| `/admin` | 后台管理 | 管理员/运营（按角色分权） |

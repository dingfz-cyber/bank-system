# 网银系统 — 完整需求文档

> 版本：v2.0  
> 最后更新：2026-06-10

---

## 一、项目概述

### 1.1 项目背景

前后端分离的在线银行展示型网银系统，面向个人/企业客户提供金融产品浏览、在线业务申请、个人信息管理等功能，同时为管理员提供完善的后台管理能力。

### 1.2 项目目标

- 搭建一个功能完整的银行展示网站，覆盖真实的业务流程
- 实现 RBAC 三级权限控制，区分管理员、运营和普通用户
- 遵循企业级后端开发规范，作为毕业设计/求职作品集项目

### 1.3 技术栈

| 层级 | 技术选型 |
|------|----------|
| 后端框架 | Java 21 + Spring Boot 3.4.4 |
| ORM | MyBatis-Plus 3.5.7 |
| 构建工具 | Maven（Maven Wrapper） |
| 前端框架 | Vue 3.5（Composition API）+ Vite 8 |
| UI 组件库 | Element Plus 2.14 |
| 状态管理 | Pinia 3.0 |
| 路由 | Vue Router 4.6 |
| HTTP 客户端 | Axios 1.16 |
| 数据库 | MySQL 8.0+（库名 `bank_demo`，字符集 `utf8mb4`） |
| 认证 | JWT（jjwt 0.12.6，密钥存环境变量） |
| 密码加密 | BCrypt（Spring Security 提供） |

### 1.4 运行环境

| 配置项 | 值 |
|--------|-----|
| 后端端口 | 8080 |
| 前端端口 | 5173 |
| MySQL 端口 | 3306 |

---

## 二、功能需求

### 2.1 用户模块

#### 2.1.1 注册
| 编号 | 需求 |
|------|------|
| U-01 | 用户通过手机号 + 密码 + 昵称注册账号 |
| U-02 | 手机号唯一校验，已注册号码不可重复注册 |
| U-03 | 密码 BCrypt 加密存储，不存明文 |
| U-04 | 新用户默认角色为普通用户（roleId=3） |

#### 2.1.2 登录 / 登出
| 编号 | 需求 |
|------|------|
| U-05 | 手机号 + 密码登录，验证失败统一提示"账号或密码错误" |
| U-06 | 登录成功后签发 JWT Token（24h 有效期），返回用户信息及 Token |
| U-07 | 前端 Token 存 localStorage，请求时通过 `Authorization: Bearer` 携带 |
| U-08 | 退出登录时前端清除 Token，服务端无状态 |

#### 2.1.3 个人中心
| 编号 | 需求 |
|------|------|
| U-09 | 查看/修改个人信息（昵称可编辑，手机号只读） |
| U-10 | 修改密码：验证原密码 → 输入新密码（6-32位）→ BCrypt 加密更新 |
| U-11 | 查看"我的申请"列表，含申请类型、审核状态（待审核/已通过/已拒绝）、备注信息 |
| U-12 | 查看"我的银行卡"：卡面展示（含掩码卡号、有效期、类型、状态标签） |
| U-13 | 冻结/解冻本人名下银行卡（需二次确认） |

### 2.2 产品模块

#### 2.2.1 产品展示
| 编号 | 需求 |
|------|------|
| P-01 | 产品按四大分类展示：个人业务（1）、信用卡（2）、公司金融（3）、普惠金融（4） |
| P-02 | 每类产品有独立页面，展示产品列表（分页，每页 12 条） |
| P-03 | 每款产品含：产品名、年化利率、产品介绍、产品图片 |
| P-04 | 产品按排序权重（sort）升序排列 |
| P-05 | 首页展示轮播图 + 四大业务分类入口卡片 + 最新公告（5条）+ 热门产品（8条） |
| P-06 | 产品搜索：按关键词模糊匹配产品名 |
| P-07 | 图片路径存相对路径，前端拼接完整 URL，加载失败显示兜底图 |
| P-08 | 产品列表支持空状态（暂无产品）和加载态 |

### 2.3 申请模块

#### 2.3.1 提交申请
| 编号 | 需求 |
|------|------|
| A-01 | 已登录用户可提交办卡申请或贷款申请 |
| A-02 | 申请表含：产品 ID、申请人姓名、手机号、申请类型 |
| A-03 | 入参校验：产品 ID 不为空、姓名不为空、手机号格式校验（1 开头 11 位） |
| A-04 | 未登录用户点击申请 → 弹窗提示 → 跳转登录页（带 redirect 参数，登录后返回） |

#### 2.3.2 审核流程
| 编号 | 需求 |
|------|------|
| A-05 | 申请提交后默认状态为"待审核"（status=0） |
| A-06 | 管理员在后台可对申请执行"通过"（status=1）或"拒绝"（status=2） |
| A-07 | 拒绝时可选填拒绝原因 |
| A-08 | 审批通过后自动记录审批人 ID 和审批时间 |
| A-09 | 办卡申请审批通过 → 系统自动生成一条银行卡记录（19 位卡号 + CVV + 3 年有效期） |

#### 2.3.3 申请管理
| 编号 | 需求 |
|------|------|
| A-10 | 管理员/运营可查看全部申请记录 |
| A-11 | 普通用户在个人中心仅可查看本人申请 |
| A-12 | 管理员可编辑、软删除申请记录 |
| A-13 | 软删除的申请进入回收站，支持恢复和物理彻底删除 |

### 2.4 银行卡模块

| 编号 | 需求 |
|------|------|
| C-01 | 银行卡含：19 位卡号、卡类型（借记卡/信用卡）、信用额度、余额、CVV、有效期、状态 |
| C-02 | 卡号生成规则：`6228`（银联 BIN）+ 时间戳后 10 位 + 5 位随机数 |
| C-03 | 信用卡默认信用额度 ¥10,000，借记卡额度为 0 |
| C-04 | 新卡默认有效期 3 年 |
| C-05 | 卡状态：正常（0）、已冻结（1）、已注销（2） |
| C-06 | 用户只能操作本人名下银行卡（后端校验 userId 归属） |

### 2.5 新闻公告模块

| 编号 | 需求 |
|------|------|
| N-01 | 新闻含：标题、摘要、正文（支持 HTML）、发布状态（草稿/已发布）、排序 |
| N-02 | 公开页面仅展示已发布（status=1）的新闻 |
| N-03 | 新闻列表分页展示，按排序升序 + 创建时间降序 |
| N-04 | 新闻列表页 `/news`（公开）+ 新闻详情页 `/news/detail`（公开） |
| N-05 | 管理员后台可新增、编辑、删除新闻 |
| N-06 | 首页展示最新 5 条已发布公告 |

### 2.6 轮播图模块

| 编号 | 需求 |
|------|------|
| B-01 | 首页轮播图支持多张图片自动播放（4s 间隔） |
| B-02 | 每张轮播图可配置跳转路由 |
| B-03 | 管理员可新增、编辑、删除轮播图 |
| B-04 | 图片存取规则同产品模块 |

### 2.7 数据看板

| 编号 | 需求 |
|------|------|
| D-01 | 管理员登录后台 → 首屏展示数据概览 |
| D-02 | 统计指标：用户总数、产品总数、今日申请数、待审核数、已通过数、已拒绝数、新闻公告数 |
| D-03 | 数据以卡片形式展示，不同颜色区分 |

### 2.8 信息公开页

| 编号 | 需求 |
|------|------|
| I-01 | 静态信息展示：公司概况、组织架构、经营范围、联系方式、服务承诺、风险提示 |
| I-02 | 组件化实现，各信息区块为独立 Vue 组件 |

---

## 三、非功能需求

### 3.1 安全需求

| 编号 | 需求 |
|------|------|
| S-01 | 所有密码 BCrypt 加密存储，不存明文 |
| S-02 | JWT 密钥统一存放环境变量，不硬编码 |
| S-03 | 数据库账号密码统一存放环境变量，不写入配置文件或代码 |
| S-04 | 接口权限校验：通过 `@RequireRole` 注解 + 角色拦截器实现 |
| S-05 | 前后端分离，Spring Security 禁用 CSRF、无状态 Session |
| S-06 | JWT 拦截器解析 Token 后设置 userId/roleId 请求属性，不拒绝请求本身 |

### 3.2 数据安全

| 编号 | 需求 |
|------|------|
| D-01 | 全部业务表使用软删除（`deleted` 字段），默认查询过滤已删除数据 |
| D-02 | 软删除的数据提供回收站查询、恢复、物理彻底删除 |
| D-03 | 用户密码字段 `@JsonIgnore`，不返回给前端 |

### 3.3 日志与审计

| 编号 | 需求 |
|------|------|
| L-01 | AOP 切面记录关键操作：注册、登录、产品增删改、申请提交/审批、数据恢复/删除 |
| L-02 | 日志含：操作描述、操作类型、操作人、IP 地址、执行耗时、成功/失败 |

### 3.4 数据校验

| 编号 | 需求 |
|------|------|
| V-01 | 全部业务接口使用 JSR 303 注解入参校验 |
| V-02 | 非法参数由全局异常处理器拦截，统一返回 `{code: 400, msg: "错误信息"}` |

### 3.5 异常处理

| 编号 | 需求 |
|------|------|
| E-01 | 全局异常处理器统一拦截：业务异常、参数校验异常、未登录异常、未知异常 |
| E-02 | 业务异常返回 HTTP 200 + 错误码，未登录返回 HTTP 401，未知异常返回 HTTP 500 |

### 3.6 代码规范

| 编号 | 需求 |
|------|------|
| R-01 | 统一返回格式 `Result<T>` {code: 200, msg: "success", data: ...} |
| R-02 | 分页统一使用 `PageResult<T>`，含 page/pageSize/total/pages/list |
| R-03 | 所有 Controller 方法必须补充标准 JavaDoc 注释 |
| R-04 | 实体类含自动填充审计字段（createTime/updateTime/createBy/updateBy） |

---

## 四、权限体系（RBAC）

### 4.1 角色定义

| 角色 | 编码 | roleId | 说明 |
|------|------|--------|------|
| 超级管理员 | SUPER_ADMIN | 1 | 全量后台管理权限 |
| 运营人员 | OPERATOR | 2 | 产品/轮播/新闻管理 + 申请查看与审批 |
| 普通用户 | USER | 3 | 仅前台浏览 + 提交申请 + 个人中心 |

### 4.2 权限矩阵

| 操作域 | SUPER_ADMIN | OPERATOR | USER |
|--------|:-----------:|:--------:|:----:|
| 产品/轮播 CRUD | ✅ | ✅ | ❌ |
| 申请查看（全部） | ✅ | ✅ | ❌ |
| 申请查看（本人） | ✅ | ✅ | ✅ |
| 申请审批 | ✅ | ✅ | ❌ |
| 新闻管理 | ✅ | ✅ | ❌ |
| 回收站操作 | ✅ | ✅ | ❌ |
| 数据看板 | ✅ | ✅ | ❌ |
| 提交申请 | ✅ | ✅ | ✅ |
| 个人中心 | ✅ | ✅ | ✅ |

### 4.3 权限实现机制

- `JwtInterceptor`：解析 JWT Token → 设置 request 的 userId/phone/roleId 属性
- `RoleInterceptor`：读取 `@RequireRole` 注解 → 对比当前用户 roleId → 判定放行/拒绝
- `@RequireRole("SUPER_ADMIN")`：仅超级管理员可访问
- `@RequireRole({"SUPER_ADMIN", "OPERATOR"})`：管理员或运营可访问
- `@RequireRole(anyRole = true)`：任意已登录用户可访问
- roleId=1（超级管理员）通行所有接口，无需逐项校验

---

## 五、数据库设计

### 5.1 表清单

| 序号 | 表名 | 说明 | 软删除 |
|------|------|------|:---:|
| ① | sys_user | 用户信息表 | ✅ |
| ② | bank_product | 产品信息表 | ✅ |
| ③ | banner | 轮播图表 | ✅ |
| ④ | apply_info | 申请表（含审核状态） | ✅ |
| ⑤ | bank_news | 新闻公告表 | ✅ |
| ⑥ | bank_card | 银行卡表 | ✅ |
| ⑦ | sys_role | 角色表 | ❌ |
| ⑧ | sys_user_role | 用户角色关联表 | ❌ |
| ⑨ | sys_menu | 菜单权限表 | ❌ |

### 5.2 通用审计字段

所有业务表统一包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| deleted | TINYINT(1) | 0=正常，1=已删除 |
| create_by | VARCHAR(64) | 创建人 |
| update_by | VARCHAR(64) | 更新人 |
| create_time | DATETIME | 创建时间，MyBatis-Plus 自动填充 |
| update_time | DATETIME | 更新时间，MyBatis-Plus 自动填充 |

### 5.3 关键表结构

#### sys_user（用户信息表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| phone | VARCHAR(20) UNIQUE | 手机号（登录账号） |
| password | VARCHAR(255) | BCrypt 加密 |
| nick_name | VARCHAR(64) | 昵称 |
| role_id | BIGINT | 角色 ID |

#### apply_info（申请表）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 申请人 ID |
| product_id | BIGINT | 产品 ID |
| real_name | VARCHAR(64) | 姓名 |
| apply_type | TINYINT(2) | 1=办卡，2=贷款 |
| status | TINYINT(2) | 0=待审核，1=已通过，2=已拒绝 |
| remark | VARCHAR(500) | 审核备注 |
| approver_id | BIGINT | 审批人 ID |
| approve_time | DATETIME | 审批时间 |

#### bank_card（银行卡表）

| 字段 | 类型 | 说明 |
|------|------|------|
| card_number | VARCHAR(19) | 19 位银联卡号 |
| card_type | TINYINT(2) | 1=借记卡，2=信用卡 |
| credit_limit | DECIMAL(12,2) | 信用额度 |
| balance | DECIMAL(12,2) | 余额 |
| cvv | VARCHAR(3) | CVV 安全码 |
| expiry_date | VARCHAR(5) | 有效期 MM/YY |
| status | TINYINT(2) | 0=正常，1=已冻结，2=已注销 |

---

## 六、接口清单

### 6.1 用户模块 `/user`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /user/register | 注册 | 公开 |
| POST | /user/login | 登录 | 公开 |
| POST | /user/logout | 退出 | 公开 |
| GET | /user/info | 获取当前用户信息 | 需登录 |
| PUT | /user/profile | 修改个人信息 | 需登录 |
| PUT | /user/password | 修改密码 | 需登录 |

### 6.2 产品模块 `/product`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /product/list | 分页查询产品 | 公开 |
| GET | /product/detail/{id} | 产品详情 | 公开 |
| POST | /product/add | 新增产品 | 管理员/运营 |
| PUT | /product/update | 修改产品 | 管理员/运营 |
| DELETE | /product/delete/{id} | 软删除产品 | 管理员/运营 |
| GET | /product/recycle | 回收站 | 管理员/运营 |
| POST | /product/recover/{id} | 恢复产品 | 管理员/运营 |
| DELETE | /product/wipe/{id} | 物理删除 | 管理员/运营 |

### 6.3 轮播图模块 `/banner`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /banner/list | 轮播列表 | 公开 |
| POST | /banner/add | 新增轮播 | 管理员/运营 |
| PUT | /banner/update | 修改轮播 | 管理员/运营 |
| DELETE | /banner/delete/{id} | 软删除轮播 | 管理员/运营 |

### 6.4 申请模块 `/apply`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /apply/submit | 提交申请 | 需登录 |
| GET | /apply/list | 申请列表 | 需登录 |
| PUT | /apply/{id} | 修改申请 | 管理员/运营 |
| DELETE | /apply/{id} | 软删除申请 | 管理员/运营 |
| POST | /apply/approve/{id} | 审批申请 | 管理员/运营 |
| GET | /apply/recycle | 回收站 | 管理员/运营 |
| POST | /apply/recover/{id} | 恢复申请 | 管理员/运营 |
| DELETE | /apply/wipe/{id} | 物理删除 | 管理员/运营 |

### 6.5 银行卡模块 `/card`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /card/list | 我的银行卡列表 | 需登录 |
| GET | /card/detail/{id} | 银行卡详情 | 需登录 |
| POST | /card/freeze/{id} | 冻结银行卡 | 需登录 |
| POST | /card/unfreeze/{id} | 解冻银行卡 | 需登录 |

### 6.6 新闻公告模块 `/news`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /news/list | 新闻列表 | 公开 |
| GET | /news/detail/{id} | 新闻详情 | 公开 |
| GET | /news/admin/list | 管理端列表（含草稿） | 管理员/运营 |
| POST | /news/add | 新增新闻 | 管理员/运营 |
| PUT | /news/update | 修改新闻 | 管理员/运营 |
| DELETE | /news/delete/{id} | 软删除新闻 | 管理员/运营 |

### 6.7 数据看板 `/dashboard`

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /dashboard/stats | 统计数据 | 管理员/运营 |

---

## 七、前端路由清单

| 路径 | 页面名称 | 权限 | 说明 |
|------|----------|------|------|
| `/` | 首页 | 公开 | 轮播 + 分类入口 + 公告 + 热门产品 |
| `/personal` | 个人业务 | 公开 | 产品类型 1 |
| `/credit` | 信用卡 | 公开 | 产品类型 2 |
| `/company` | 公司金融 | 公开 | 产品类型 3 |
| `/puhui` | 普惠金融 | 公开 | 产品类型 4 |
| `/info` | 信息公开 | 公开 | 公司概况/组织架构等 |
| `/news` | 新闻公告 | 公开 | 新闻列表 |
| `/news/detail` | 新闻详情 | 公开 | 按 query.id 加载 |
| `/login` | 登录 | 公开 | — |
| `/register` | 注册 | 公开 | — |
| `/apply/card` | 办卡申请表 | 需登录 | — |
| `/apply/loan` | 贷款申请表 | 需登录 | — |
| `/user/center` | 个人中心 | 需登录 | 信息/密码/申请/银行卡 |
| `/admin` | 后台管理 | 管理员/运营 | 看板/产品/轮播/申请/新闻/回收站 |

---

## 八、前端项目结构

```
frontend/src/
├── api/                          # API 接口封装
│   ├── request.js                # Axios 实例（拦截器：Token 注入 + 401/403 处理）
│   ├── user.js                   # 注册/登录/修改信息/改密
│   ├── product.js                # 产品 CRUD + 回收站
│   ├── banner.js                 # 轮播 CRUD
│   ├── apply.js                  # 申请 CRUD + 审批
│   ├── card.js                   # 银行卡查询/冻结/解冻
│   ├── news.js                   # 新闻 CRUD
│   └── dashboard.js              # 统计看板
├── assets/
│   └── styles/index.css          # 全局样式 + CSS 变量体系
├── components/                   # 公共组件
│   ├── AppHeader.vue             # 顶部导航（路由链接 + 登录态 + 个人中心/后台入口）
│   ├── AppFooter.vue             # 底部信息
│   ├── ProductList.vue           # 产品列表（分页 + 搜索 + 空/加载态）
│   └── info/                     # 信息公开子组件（6 个）
├── composables/
│   └── use-image.js              # 图片 URL 拼接 + 错误兜底
├── router/
│   └── index.js                  # 路由配置 + 全局导航守卫
├── stores/
│   └── user.js                   # Pinia 用户状态（Token/昵称/角色 + localStorage）
├── utils/
│   ├── constants.js              # 全局常量 + 映射表
│   └── index.js                  # 工具函数
└── views/                        # 页面（每页面一个 index.vue）
    ├── home/                     # 首页
    ├── personal/                 # 个人业务
    ├── credit/                   # 信用卡
    ├── company/                  # 公司金融
    ├── puhui/                    # 普惠金融
    ├── info/                     # 信息公开
    ├── news/                     # 新闻列表 + 详情
    ├── login/                    # 登录
    ├── register/                 # 注册
    ├── apply/                    # 申请表
    ├── usercenter/               # 个人中心
    └── admin/                    # 后台管理（tab 式多模块）
```

---

## 九、后端项目结构

```
backend/src/main/java/com/bank/
├── BankApplication.java          # Spring Boot 入口
├── common/                       # 通用封装
│   ├── Result.java               # 统一返回 {code, msg, data}
│   ├── PageResult.java           # 分页包装 {page, pageSize, total, pages, list}
│   ├── BusinessException.java    # 业务异常（自定义 code + msg）
│   ├── NotLoginException.java    # 未登录异常（HTTP 401）
│   └── GlobalExceptionHandler.java # 全局异常处理
├── config/                       # 框架配置
│   ├── SecurityConfig.java       # Spring Security（无状态 + 全路径放行）
│   ├── WebConfig.java            # CORS 跨域
│   ├── WebMvcConfig.java         # 拦截器注册 + 静态资源映射
│   ├── JwtInterceptor.java       # JWT 解析拦截器
│   ├── JwtUtil.java              # JWT 生成/解析工具
│   ├── MyBatisPlusConfig.java    # MyBatis-Plus 分页插件
│   └── MyMetaObjectHandler.java  # 审计字段自动填充
├── rbac/                         # 权限控制
│   ├── RequireRole.java          # 角色注解（支持多角色 + anyRole）
│   └── RoleInterceptor.java      # 角色校验拦截器
├── log/                          # 操作日志
│   ├── OperationLog.java         # 操作日志注解
│   └── OperationLogAspect.java   # AOP 切面（记录操作人/IP/耗时/结果）
└── module/                       # 业务模块
    ├── user/                     # 用户（注册/登录/信息/改密）
    ├── product/                  # 产品（CRUD + 回收站）
    ├── banner/                   # 轮播（CRUD）
    ├── apply/                    # 申请（提交/列表/审批/回收站）
    ├── card/                     # 银行卡（列表/详情/冻结/解冻）
    ├── news/                     # 新闻（公开列表/管理CRUD）
    └── dashboard/                # 数据看板（统计查询）
```

---

## 十、业务流程

### 10.1 用户办卡流程

```
用户注册 → 登录 → 浏览信用卡产品 → 点击"立即申请"
→ 填写申请表（姓名/手机号）→ 提交（status=0 待审核）
→ 管理员后台查看申请 → 点击"通过"（status=1）
→ 系统自动生成银行卡（19位卡号 + CVV + 有效期）
→ 用户在个人中心 → 我的银行卡 → 看到新卡
→ 用户可冻结/解冻银行卡
```

### 10.2 管理员后台流程

```
管理员登录 → 后台管理首页（数据概览看板）
→ 产品管理 tab：新增/编辑/删除产品
→ 轮播管理 tab：新增/编辑/删除轮播图
→ 申请记录 tab：查看全部申请 → 通过/拒绝
→ 新闻管理 tab：新增/编辑/删除新闻
→ 回收站 tab：恢复或彻底删除已删数据
```

### 10.3 数据删除回收流程

```
管理员删除操作 → 触发表逻辑删除（deleted=1）
→ 数据移至回收站 → 管理员可恢复（deleted=0）或物理删除（DELETE FROM）
```

---

## 十一、开发约束

1. **文档先行**：表结构/接口/业务逻辑调整 → 先修订设计文档 → 再改代码
2. **软删除**：全部业务表使用逻辑删除，配套回收站 + 物理删除
3. **统一返回**：所有接口返回 `Result<T>`，分页使用 `PageResult<T>`
4. **参数校验**：全部接口使用 JSR 303 注解校验，非法参数直接拦截
5. **日志记录**：关键操作（注册/登录/增删改/审批/恢复/删除）AOP 自动记录
6. **敏感信息**：密码/密钥/数据库连接存环境变量，禁止硬编码
7. **图片资源**：代码只写路径占位，物理文件由使用者自行放入对应文件夹
8. **单模块自测**：每完成一个模块进行接口测试 + 功能验证
